{# IO sys #}
	read 	:([read])
	write	:([write])
{# Math #}
	+		:([add])
	-		:([sub])
	*		:([mul])
	/		:([div])
#	++		:([operation ])
#	--		:([operation ])
#	**		:([operation sq])
#	^		:([operation exp])
{# Boolean #}
	>		:([more])
	<		:([less])
	=		:([equal])
	<=		:([more_or_equal])
	>=		:([less_or_equal])
	!=		:([not_equal])
{# Stack #}
	pop		:([pop])
	dup		:([dup])
	dup_1	:([dup 1])